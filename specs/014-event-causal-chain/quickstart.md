# Quickstart: Event Causal Chain

Select an event in Timeline and press `f`. RedLog loads the bounded causal
component from both event tiers, merges available events into the Timeline and
shows whether references are unavailable or the traversal reached a safety bound.
