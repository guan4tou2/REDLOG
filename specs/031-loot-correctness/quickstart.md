# Quickstart: Loot Detection Correctness

1. In the built-in terminal, `cat` a file containing an AWS key twice; open both
   command rows and confirm the key is masked in each; the Loot page lists it once.
2. Run `redlog-session bash`, print the same key, and confirm a loot row caused
   by the session output row.
