# Tasks

- [x] T001 Add failing boundary tests for distinct adapters, shared transport and removal of combined entry points. RED: all three boundary cases failed against the combined implementation.
- [x] T002 Extract the POSIX common runtime and add thin bash/zsh lifecycle adapters.
- [x] T003 Extend starter-pack and hook installation with colocated support files and fallback parity.
- [x] T004 Route WSL, built-in terminal and wrapper callers directly to shell-specific adapters; remove historical combined entry points.
- [x] T005 Verify adapter/output/hook contracts (74 focused tests plus 30 integration tests), bash/zsh syntax, typecheck, build and Electron setup flow (6 tests).
