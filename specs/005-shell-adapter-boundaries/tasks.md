# Tasks

- [x] T001 Add failing boundary tests for distinct adapters, shared transport and compatibility delegation. RED: all three boundary cases failed against the combined implementation.
- [x] T002 Extract the POSIX common runtime and add thin bash/zsh lifecycle adapters.
- [x] T003 Extend starter-pack and hook installation with colocated support files and fallback parity.
- [x] T004 Convert historical entry points to compatibility delegators, auto-upgrade recognised legacy copies, and preserve WSL/built-in callers.
- [x] T005 Verify adapter/output/hook contracts (43 tests), bash/zsh syntax, typecheck, build and Electron setup flow (6 tests).
