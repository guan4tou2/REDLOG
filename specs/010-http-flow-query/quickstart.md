# Validation Quickstart

1. Seed more flows than one page with request and response rows.
2. Confirm every page contains complete flows and no duplicate flow ID.
3. Enable scope filtering and confirm it applies before the page limit.
4. Load more in HTTP History and confirm all three projections gain the flows.
5. Run full tests, build and the HTTP activity Electron journey.
