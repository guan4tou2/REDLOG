# Quickstart: Command Artifact Linking

1. Enable file monitoring for a narrow working directory.
2. Run a recorded command that writes a file below that directory.
3. Open the file event and use causal focus to reach the command.
4. Confirm simultaneous ambiguous commands remain unlinked.
