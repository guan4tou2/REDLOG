# Tasks: One Retention Model

- [x] T001 Add failing tests: declared shape, no knob outside `retention`, no
      old spelling in `src/`, an old config deletes nothing.
- [x] T002 Move the sweep suites' config literals to the new shape.
- [x] T003 Move the config type and defaults.
- [x] T004 Point the three sweeps at `retention.<store>`.
- [x] T005 Point the Settings size-budget fields at `retention.<store>`.
- [x] T006 Rewrite `docs/TESTING.md` §2.6; update live design docs.
- [x] T007 Run the retention suites, typecheck, the full suite and build;
      record verification.
