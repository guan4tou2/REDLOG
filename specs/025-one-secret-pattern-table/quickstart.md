# Quickstart: One Secret Pattern Table

1. Open `src/core/secret-patterns.ts` and compare `REDACT_IN_TRANSCRIPTS` with
   `DETECT_AS_LOOT` — every coverage difference is there.
2. Paste an AWS key into a recorded command's output and confirm the same loot
   event as before.
3. Confirm an AI transcript line containing a GitHub token is still masked.
