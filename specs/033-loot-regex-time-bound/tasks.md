# Tasks: Loot Rule Time Bound

- [x] T001 Add failing tests: overrun rule stopped with others still matching; stop reported and not re-run; reload clears it.
- [x] T002 `core/bounded-regex.ts`: worker, ready flag, bounded synchronous run.
- [x] T003 Run plugin rules through it in `loot-detector`; stop and retry.
- [x] T004 Report `stopped` in `listLootRules()`; mark it in `LootRulesGroup` with i18n; render test.
- [x] T005 Update the plugin guide and CHANGELOG.
- [x] T006 Run loot suites, full suite, typecheck, build; record verification and gates.
