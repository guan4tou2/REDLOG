# Quickstart: Tailer Naming

1. Open Settings ▸ Capture on Windows, turn on "Follow PowerShell
   Start-Transcript", save, and confirm `config.yaml` has
   `powershellTranscript.enabled: true`.
2. With the agent tailer enabled, run a Claude Code session in a project
   directory and confirm `agent.*` events arrive as before.
