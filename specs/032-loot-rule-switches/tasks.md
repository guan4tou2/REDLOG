# Tasks: Loot Rule Switches

- [x] T001 Add failing tests: default list, rule ids, masking kept, plugin rule off, no row when off.
- [x] T002 Rule ids on matches; `disabledRules` in the detector; `listLootRules()`.
- [x] T003 Config type and default; apply on open and save.
- [x] T004 `loot:rules` IPC, preload, env types.
- [x] T005 `LootRulesGroup` in Settings ▸ Capture with i18n; render tests.
- [x] T006 TESTING.md option row and CHANGELOG; run suites, typecheck, build.
