# Validation Quickstart

1. Spawn an 80x24 built-in terminal and resize it to 120x40.
2. Confirm the `.cast` contains a valid resize frame after its header.
3. Confirm command output still receives a valid byte range and replays.
4. Run terminal tests, typecheck, production build and command-I/O E2E.
