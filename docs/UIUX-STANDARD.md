# RedLog UI/UX 標準

從 `src/renderer` 的實際程式碼提取現有設計風格，據此建立統一標準。設計稿與完整論述見專案內的
「RedLog 設計規範」（1a 規範／1b 現況重建／1c 改良對照）。本檔是工程實作用的摘要與檢查表。

- 對照基準：`main` @ v0.14.3 + 未發布 commit（原生右鍵選單、殼層不可選取）
- 語言：介面全繁體中文，僅保留無法翻譯的技術詞
- 密度：預設「寬」，另提供「緊密」；與既有 `--app-zoom` 正交

---

## 1. Tokens

| token | 現況 | 標準 | 用途 |
|---|---|---|---|
| `bg` | `#0a0a0a` | `#121214` | 視窗底、側欄、標題列 |
| `surface` | `#141414` | `#1a1a1d` | 卡片、面板 |
| `elevated` | `#1a1a1a` | `#212126` | 輸入框、chip、hover |
| `border` | `#262626` | `#33333a` | 卡片與分隔線 |
| `border-subtle` | `#1e1e1e` | `#26262c` | 列間分隔 |
| `text` | `#e5e5e5` | `#ececf0` | 主要內容、數值 |
| `text-dim` | `#a1a1aa` | `#9a9aa4` | 標籤、說明（AA 下限 7.1:1） |
| `text-faint` | — | `#7a7a84` | 分區標題、次要說明 |
| `muted` | `#71717a` | `#6a6a74` | placeholder、停用 |
| `accent`（品牌紅） | `#d75f63` | `#d75f63` | 導航選取、區塊標題、主要按鈕 |
| `danger`（危險紅） | 與品牌紅同色 | `#ff4d4f` | 實心底 + 白字，僅危險 |
| `lane` | 18 種色相 | `#6e6e78` | 全部泳道統一 |
| `safe` / `warn` / `info` | `#5ecf9c` / `#d4ac5a` / `#3fc7d6` | 不變 | 狀態 |

**紅色語意收緊**：品牌紅永遠只做文字或細線；危險紅永遠是實心色塊。全 app 同時只允許一處危險紅。

**狀態唯一真相表**

| 狀態 | 色彩 | 唯一允許的表現 |
|---|---|---|
| 正常／安全 | `#5ecf9c` | 圓點 + 文字，無實心底、無動畫 |
| 未知／閒置 | `#d4ac5a` | 圓點 + 文字 + 一句「怎麼補」 |
| 危險／違規 | `#ff4d4f` | 實心徽章或實心按鈕，白字 |
| 錄製中 | `#d75f63` | 閃爍圓點，僅狀態列與 HUD |
| 資訊／連線 | `#3fc7d6` | HUD 邊框、拓樸、minimap；不用於狀態判定 |

狀態絕不只靠顏色——圓點必須配文字或形狀。

## 2. 字型與字級

- 介面字 `Noto Sans TC`（隨 app 打包，三平台一致）；等寬字 `JetBrains Mono`
- IP、hash、exit code、時間、事件數一律等寬 + `tabular-nums`
- 硬下限 **13px**（現有約 155 處 `text-[10px]` / `text-[11px]` 需清除）
- 數值 22px/600 · 畫面標題 17px/600（不再全大寫）· 內文 15px/1.7 · 分區標題 13px/600 tracking .14em
- HUD 是唯一例外：下限 11px（有自己的縮放設定）

## 3. 密度（CSS 變數，不碰 Tailwind 類名）

| 變數 | 寬（預設） | 緊密 | 套用 |
|---|---|---|---|
| `--row-h` | 40px | 32px | 側欄項、泳道列、清單列 |
| `--pad` | 16px | 10px | 卡片與面板內邊距 |
| `--gap` | 12px | 8px | 卡片、chip 之間 |
| `--section-gap` | 24px | 14px | 儀表板區塊之間 |

`:root[data-density='tight']` 覆寫。實際點擊區在緊密模式仍不得低於 32px。
字級調大時自動切到「緊密」，保住一屏資訊量。

## 4. 元件

- **按鈕**四級（主要／次要／安靜／危險），高 34px（緊密 30px），圓角 8px，focus 環 2px `#d75f6366` 外偏移 2px。同一畫面最多一個主要
- **卡片**：取消頂部 2px 色條，改為左側色塊包住圖示（危險用實心，其餘用 12% 底色，與徽章規則一致）。卡片底色永遠是 `surface`，不因狀態改變
- **徽章**：12% 底色 + 同色文字 + 圓點；只有危險用實心
- **chip**：選取時 `elevated` + 1px 邊框，不用紅色
- **輸入框**：placeholder `#6a6a74`；IP/CIDR/路徑類一律等寬
- **圖示**：Lucide，1.5px stroke，尺寸只用 16/20/24px（取代 `◉ ▸ ═ ☰ ◻ ⊕ ⊘ ◆ ⚑`，這些 Unicode 在 Windows 落到不同 fallback）
- **載入**：<300ms 不顯示 · <2s 行內 spinner · >2s 骨架屏 + 進度文字（骨架尺寸須與真實內容對齊）

## 5. UX 原則

1. 一屏只回答一個問題——每畫面選一個主問題給最大字級，其餘降為單行摘要
2. 例外報告，不是清單——預設只顯示異常，正常收成一行 +「顯示全部（N）」
3. 導航順序固定，⌘1..9 永遠對應同一畫面，數字印在側欄右側
4. 空狀態三件套：會出現什麼／為什麼現在沒有／一顆按鈕
5. 危險操作分三級：可逆→直接執行 + Toast 附復原；不可逆→對話框 + 勾選框；影響證據鏈→輸入專案名確認 + 列出具體後果
6. 全中文介面，技術詞白名單：`IP · CIDR · SHA-256 · JWT · DNS · HTTP · mitmproxy · MCP · OpenTimestamps · CDP · WSL · hook · token · shell`。固定譯名：Loot→戰利品、Scope→範圍、Marker→標記、Pivot→中繼跳板、Transcript→逐字稿、Anchor→錨定、Chain→證據鏈
7. 符合 macOS 慣例：設定為左清單 + 右內容；⌘. 暫停、⌘K 命令面板、⌘F 頁內搜尋、Esc 逐層退出；破壞性動作在對話框右下（Windows 自動對調）；tooltip 不得是唯一的資訊來源
8. 可及性硬門檻：文字 ≥4.5:1、非文字狀態元素 ≥3:1、focus 環處處可見、`prefers-reduced-motion` 保留

## 6. 時間軸

- **兩種形態**：可視範圍內任一泳道 >50 筆 → 全部泳道一起折疊成事件流（保持時間對齊）；使用者手動改過模式後，本次 session 不再自動切換
- **事件流每列**：時間戳 + tier glyph + 泳道名 + 一行摘要。保留泳道篩選（逐字稿則固定只看指令與對話）
- **Inspector**：右側 380px 可拖抽屜，三分頁〈詳細／相關／原始〉。「相關」就是全局因果鏈面板，不另建。預設關；視窗 >1600px 預設開
- **滑鼠**：滾輪捲時間；Ctrl/⌘+滾輪與雙指開合**以游標為錨**縮放（不是畫面中心）；雙擊空白處縮放到前後 5 分鐘
- **鍵盤**：← → 同泳道走；↑ ↓ 跨泳道落在時間最接近的事件；Shift+← → 跳下一筆帶狀態的事件；Home/End；+ − / 0 以選中事件為錨；Enter 開關 Inspector
- 鍵盤移動必須有可見選取框（2px 品牌紅外環 + 事件點 1.3×）；自動捲入視野**不得改變縮放層級**
- **檢視模式**（取代工具列八組並列開關）：作業中（收合代理逐輪 + 略過閒置 + 本機時區）／稽核（只上鏈事件 + 異常標記 + UTC + 不壓縮時間，**鎖定不可微調**）／除錯（全部事件 + session 邊界 + 原始時間）

## 7. 跨頁互連

每條跳轉都必須雙向且可返回；跳轉後頂部出現麵包屑〈← 回到目標 · 10.10.11.24〉，Esc 或點擊回原位置與原捲動點。

| 來源 | 可展開 | 跳到哪裡 |
|---|---|---|
| 目標 | 每條指令（時間、結束碼、operator） | 每條指令單獨跳時間軸並選中該事件 |
| 範圍違規 | 命中哪條規則、root domain 比對過程、完整指令 | 時間軸範圍泳道；可再跳觸發指令 |
| 戰利品 | 每筆命中的類型、來源串流、信心度 | **來源指令**，非只跳時間點 |
| 逐字稿 | 預設只顯示輸入 + 輸出首行；結束碼非 0 或有戰利品則自動展開 | 雙向（Inspector 有〈在逐字稿上顯示〉） |

## 8. HUD

- 排版：左端兩點（錄製、安全狀態）→ 外部 IP（最大字級）→ 內部 IP 右對齊
- 位置：頂部置中**偏右**（避開 MacBook notch，靠近選單列狀態區）；每台螢幕分別記住位置
- 透明度：預設 0.85，游標移入轉 1.0（因此**滑點預設關**）
- 展開態四段固定順序：鍵值表 → 網路拓樸 → 警告列（僅未知 IP 或錯誤）→ 動作列（快速標記／仔細標記／固定）。標籤欄寬隨字級縮放
- 跳板節點：收合態在外部 IP 下方長出青色切角膠囊（最新節點 + `+N`）；展開態按時間把每一跳插進鏈中。**這是唯一允許在收合態長高 HUD 的元素**
- 穿透按鈕：放展開態動作列；必須有 `⌘⇧P` / `Ctrl Shift P` + 選單列作為不靠點擊的退出，並在開啟瞬間浮出 3 秒提示；穿透中整體降透明、四角改虛線，但外部 IP 維持不透明
- IP 暴露時強制解除穿透、轉為不透明、固定展開——唯一允許覆寫使用者偏好的情況

## 9. 錯誤、狀態與資料量

- **錯誤訊息三段式**：發生什麼事（中文）+ 為什麼 + 一顆動作；原始錯誤收在「詳細」折疊區
- **Toast**：錯誤不自動消失；同類合併計數；上限 3 則；容器需 `aria-live="polite"`（錯誤用 `assertive`）
- **持續性故障**分兩層固定在狀態列左側：〈需要注意 N〉只收影響證據的（抓取中斷、錨定失敗、鏈斷、DB 寫入錯誤、檔案遺失）—— **不可忽略**、紅點；其餘收〈待處理〉—— 可忽略、灰色、不寫稽核
- **時間**：一律 24 小時。相對時間**只用於新鮮度欄位**（最後一次抓取、3 秒前）；事件本身、時間軸軸標、逐字稿、匯出一律絕對時間，匯出用 ISO 8601
- **文字截斷**：一律可 hover 看全文（原生 `title`），長內容另可展開成多行
- **大量資料**：全部改無限捲動 + 虛擬列表，底部顯示「已載入 N／共 M」。目標頁改 SQL 聚合（`GROUP BY detectedTarget`），不再讀 1000 筆原始事件在前端聚合
- **致命錯誤畫面**：錯誤摘要 + 〈重試〉〈回儀表板〉〈複製診斷資訊〉+〈開啟 GitHub issue〉。診斷資訊預設只帶版號、OS、畫面名、stack；送出前顯示完整預覽；路徑收成 `app.asar` 之後的相對路徑，使用者名與專案名換成 `<user>` / `<project>`
- **清單頁鍵盤**：方向鍵走、Enter 展開／進入、⌘↩ 跳時間軸、Esc 退出——全部清單頁一致
- **對話框**：需 focus trap（目前三個 `role="dialog"` 只有 autofocus）

## 10. 刪除與修正的功能

- **狀態列〈⏱ 時戳〉刪除** —— 與標記寫入同一張表，只差沒有標題與嚴重度，功能重複。改由標記對話框提供「快速落點」（HUD 已是此模型）
- **側欄搜尋頁刪除** —— 全部收進 ⌘K（導航、事件／標記／operator／host 模糊搜尋、動作、檢視模式與密度切換、專案切換、最近指令）。頁內篩選單獨給 ⌘F。設定項不進 ⌘K，由設定頁自己的搜尋框負責
- **〈顯示 N 個機密〉與本機遮蔽全部刪除** —— 資料已在使用者自己的機器上。**例外：進入證據包、任何匯出、雲端分享、藍隊 webhook 的資料仍須經 redaction**，這條線要在程式裡分開實作
- **Token 不再靠使用者複製** —— 寫入 `~/.redlog/tokens/`（與專案分離，不會被證據包或匯出收進去）並在 Finder／檔案總管點亮
- **外掛信任** —— 一張對話框列出每項權限的具體影響 + 內容雜湊指紋 + 勾選框；「一鍵信任 registry 建議的發行者」改為逐一信任，並列出該發行者能簽署的外掛數
- **範圍頁重做** —— 現況顯示證據鏈筆數（與範圍無關，屬儀表板），卻缺少「允許清單是什麼、哪條規則命中」。改為：允許清單 + 排除清單（可直接編輯）+ 可展開的違規清單
- **匯出六處收成一個下拉** —— 工具列右上〈匯出 ▾〉：當前頁面／可視切片／全部；JSON 或 Markdown；另加〈證據包（含驗證器）〉。每個選項下方一行預覽「將匯出 412 筆事件、約 2.1 MB」
- **終端機**：分頁顯示 cwd 而非 `term-1`；錄影中顯示 `.cast` 大小與上限進度；字級改 ⌘+／⌘−；搜尋改 ⌘F 並顯示符合筆數；關閉分頁改為可復原
- **回放器**：浮在狀態列上方的抽屜（高 56px），只在回放中出現，切畫面不中斷播放
- **可復原（Toast 附復原，8 秒）**：關閉終端分頁、暫停錄製、移除 hook、停用外掛、將目標加入允許範圍。後三項採延遲執行——8 秒內復原則不寫入 `system.config_changed`
- **右鍵落標記改為右鍵菜單裡的一項**〈在此落標記〉—— 主程序的 `context-menu` 事件不受 renderer 的 `preventDefault` 影響，現況會同時落標記與開複製菜單，而那筆標記已寫進證據鏈

## 11. 已知的既存缺陷（非設計問題）

- `v0.14.0` per-row tier glyph 的已上鏈列用 `zinc-700` (`#3f3f46`) 於 `#0a0a0a`，約 **1.7:1**，遠低於 3:1。依例外報告原則：已上鏈是預設不畫字符，只有 logged 列畫 `⌇`，用 `#9a9aa4`
- CDP 設定指引裡的 `--remote-debugging-port=9222` 是硬寫的，使用者改過埠號後照抄會失敗——指令中的路徑與埠號必須是當前專案的實際值
- 快捷鍵表分散硬寫在監聽器、側欄、儀表板與 `?` 面板四處，已經漂移過一次（Settings 鎖定 ⌘9 那次）。須從單一來源（`lib/shortcuts.ts`）產生

---

# 實作檢查表（三期，帶依賴）

三期 = 三個 PR，每期一次大合併。第一期改 tokens 會同時動到數十個檔，拆太細會出現新舊並存的中間狀態。

## 第一期 · Tokens 與字型（無前置依賴）

- [ ] `tailwind.config.js`：`redlog` 命名色改為新值，新增 `danger` / `danger-hover` / `lane` / `text-faint`
- [ ] `tailwind.config.js`：`fontFamily` 加入 `Noto Sans TC` / `JetBrains Mono`；打包字型檔
- [ ] `tailwind.config.js`：`fontSize` 的 `xs`/`sm`/`base` 抬到 13/15/17px
- [ ] `index.css`：`html` 根字級 17px → 16px（字級下限已在 `fontSize` 抬過一次，兩處疊加會過大）
- [ ] `index.css`：body 背景與文字改新 token；保留既有的 `user-select` 殼層規則不動
- [ ] `index.css`：新增 `--row-h` / `--pad` / `--gap` / `--section-gap` 與 `[data-density='tight']`
- [ ] `index.css`：捲軸 6px → 10px
- [ ] 清除全部 `text-[10px]` / `text-[11px]`（約 155 處），HUD 例外保留至 11px
- [ ] 次級文字一律 `text-redlog-text-dim`，不得再用 `zinc-600` / `zinc-700` 承載可讀文字
- [ ] `Timeline.tsx`：`LANE_COLORS` 全部改 `#6e6e78`；狀態色改由事件本身承載
- [ ] `lib/hud.ts`：`muted` 由 `#5f7a82` 抬到可讀值；HUD 標籤字下限 11px
- [ ] 導入 Lucide，替換側欄與元件的 Unicode 幾何符號（狀態／標記類保留符號者需個別確認）
- [ ] `TierBadge`：已上鏈列不畫字符，logged 列用 `#9a9aa4`

**驗收**：`vitest` 全綠 · `playwright` 全綠 · 三平台截圖人工目視 · 新增對比度與最小字級的回歸測試（沿用 `test/lane-colours.test.ts` 從原始碼解析斷言的作法）

## 第二期 · 行為規則（依賴第一期的 tokens 與字級）

- [ ] 錯誤訊息三段式；`toast(e.message)` 全部改掉（約 50 處）
- [ ] Toast：錯誤不自動消失、同類合併、上限 3、`aria-live`
- [ ] 狀態列〈需要注意 N〉／〈待處理〉兩層指示器
- [ ] 時間格式統一；相對時間限縮到新鮮度欄位；匯出 ISO 8601
- [ ] 文字截斷一律附 `title`；長內容可展開
- [ ] 危險操作三級；`ConfirmDialog` 加勾選框與輸入確認兩種型態 + focus trap
- [ ] 可復原動作改 Toast 附復原（含三項延遲執行）
- [ ] 清單頁鍵盤操作（方向鍵／Enter／⌘↩／Esc）
- [ ] 時間軸滑鼠縮放改以游標為錨；補雙指開合與雙擊縮放
- [ ] 時間軸鍵盤移動（← → / ↑ ↓ / Shift+← → / Home End / + − 0 / Enter）+ 可見選取框
- [ ] 快捷鍵表集中到 `lib/shortcuts.ts`，補齊 Windows/Linux 欄，餵給監聽器／側欄／儀表板／`?` 面板
- [ ] HUD 透明度、位置（頂部置中偏右）、每螢幕記憶、穿透按鈕 + `⌘⇧P` 退出
- [ ] 右鍵落標記改為右鍵菜單項
- [ ] 致命錯誤畫面 + 診斷資訊清洗（路徑相對化、`<user>` / `<project>`）+ 送出前預覽
- [ ] 刪除〈顯示 N 個機密〉與本機遮蔽；確認匯出路徑的 redaction 仍在
- [ ] Token 改寫入 `~/.redlog/tokens/` 並在檔案管理員點亮
- [ ] 擷取來源的雙軸控制與狀態組合對應的單一主要動作；安裝失敗顯示真正原因；指令中的埠號與路徑取當前專案實際值

**驗收**：同上 + 錯誤路徑的 e2e（安裝失敗、錨定失敗、鏈斷）· 鍵盤操作 e2e · 無障礙檢查（focus trap、`aria-live`）

## 第三期 · 結構重整（依賴第二期的行為規則）

- [ ] 側欄順序固定、數字上欄、移除拖曳排序（或改為釘選）
- [ ] 設定改左清單 + 右內容；外掛／市集／發行者／撤銷清單從四層攤平到兩層；頂部搜尋框
- [ ] 側欄搜尋頁刪除；⌘K 命令面板（導航／模糊搜尋／動作／模式與密度／專案／最近指令）
- [ ] ⌘F 頁內篩選
- [ ] 儀表板改「一屏一個問題」+ 寬屏 1400px 居中
- [ ] 時間軸事件流模式 + 折疊門檻 + 模式列
- [ ] 時間軸 Inspector 三分頁；工具列八組開關收成檢視模式下拉；稽核 chip 併入稽核模式
- [ ] 跨頁互連五條 + 麵包屑返回
- [ ] 逐字稿新預設（首行預覽 + 條件自動展開）
- [ ] 範圍頁重做（允許清單 + 排除清單 + 可展開違規）
- [ ] 目標頁改 SQL 聚合
- [ ] 全部清單改無限捲動 + 虛擬列表 +「已載入 N／共 M」
- [ ] 匯出六處收成一個下拉
- [ ] 終端機分頁資訊、⌘+／⌘−、⌘F、可復原關閉
- [ ] 回放器改狀態列上方抽屜
- [ ] 空狀態三件套套用到全部空狀態
- [ ] 外掛信任流程改逐一信任 + 權限清單 + 指紋
- [ ] 卡片左側色塊改版（影響所有卡片）
- [ ] 移除狀態列〈⏱ 時戳〉

**驗收**：同上 + 資訊架構走查（每個設定項 ≤2 層可達）· 虛擬列表在 28k 事件下的捲動效能 · 全畫面截圖比對
